{
  "name": "base-wasm",
  "version": "0.1.0",
  "main": "index.html",
  "devDependencies": {
    "vite": "^2.6.14",
    "vite-plugin-wasm-pack": "^0.1.9"
  },
  "engines": {
    "node": ">=15.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build-js": "vite build",
    "clean-js": "rm -rf dist",
    "build-wasm": "wasm-pack build ./src-wasm --target web",
    "clean-wasm": "rm -rf ./src-wasm/pkg"
  }
}
